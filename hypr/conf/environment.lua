-- Переменные наследуются новыми приложениями. После изменений выйди
-- из сессии и войди снова: уже запущенные программы их не перечитают.
-- Здесь предусмотрен обычный запуск Hyprland; настройка UWSM — отдельный шаг.

hl.env("XCURSOR_SIZE", "24")
hl.env("HYPRCURSOR_SIZE", "24")

-- Для настольного ПК с NVIDIA RTX 5060 и установленным драйвером NVIDIA.
-- Это выбор GLX-провайдера для XWayland-приложений, а НЕ установка драйвера.
hl.env("__GLX_VENDOR_LIBRARY_NAME", "nvidia")

-- Добавить только после установки и настройки nvidia-vaapi-driver:
-- hl.env("LIBVA_DRIVER_NAME", "nvidia")

-- Стандартные механизмы Wayland сохраняем; GDK_BACKEND, GBM_BACKEND,
-- старые WLR_* и флаги Electron глобально не задаём.
-- Для конкретного проблемного приложения флаги добавляются в его команду.

hl.config({
    cursor = {
        -- 2 = автоматический выбор аппаратного/программного курсора.
        -- Если курсор пропадает, для диагностики попробуй 1 и затем верни 2.
        no_hardware_cursors = 2,
    },
})
