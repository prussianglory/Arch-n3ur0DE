-- Последний подключаемый файл для твоих небольших дополнений.
-- Для обычных значений hl.config более позднее значение перекрывает раннее.
-- Например, отключить анимации:
-- hl.config({ animations = { enabled = false } })
--
-- ВАЖНО: новые bind и rule добавляются, а не заменяют старые автоматически.
-- Чтобы поменять существующий хоткей, редактируй conf/bindings.lua.
-- Чтобы поменять команды приложений/палитру — conf/settings.lua.
-- Само изменение таблицы settings здесь не пересоздаст уже объявленные бинды.
